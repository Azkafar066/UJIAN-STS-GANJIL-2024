// main.js

// Mengimpor fungsi dari exam.js
const kelompokkanBilangan = require("./Exam.JS");

// Loop untuk iterasi dari 1 hingga 20
for (let i = 1; i <= 20; i++) {
    console.log(`${i}: ${kelompokkanBilangan(i)}`);
}s
